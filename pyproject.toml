[project]
name = "nurion"
version = "0.1.0"
description = "Nurion data platform workspace"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "aether",
    "solstice",
]
